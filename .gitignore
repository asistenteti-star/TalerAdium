# Vercel
.vercel

# Entorno local — nunca subir credenciales del Sheet
.env
.env.local
.env*.local

# Dependencias
node_modules/

# Ruido del sistema y de herramientas
.DS_Store
Thumbs.db
*.log
snowflake.log

# PDFs que genera la propia app al imprimir: útiles para revisar, no para
# versionar. Los materiales del taller viven en assets/docs y sí se versionan.
/docs/*.pdf
